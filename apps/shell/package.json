{
  "name": "shell",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "pnpm build && wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@family-tools/ui": "workspace:*",
    "hono": "^4.6.0",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.52.1",
    "@preact/preset-vite": "^2.9.0",
    "vite": "^6.0.0",
    "wrangler": "^4.123.0"
  }
}
