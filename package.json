{
  "name": "microapps",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "pnpm --filter shell dev",
    "build": "pnpm -r build",
    "check": "tsc --build && oxlint .",
    "typegen": "pnpm -r run types"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "oxlint": "^1.0.0",
    "typescript": "^7.0.0",
    "wrangler": "^4.123.0"
  }
}
